name: jsnail
description: A utility class for parsing values from dynamic sources such as Maps.
version: 0.0.3
homepage: https://github.com/Snailapp

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows: